Desculpe, mas como sou uma IA de texto, não consigo gerar um código em Smalltalk aqui. No entanto, posso fornecer um exemplo de código em Smalltalk para você:

```
| contador |

contador := 1.

[contador <= 10] whileTrue: [
    Transcriber show: contador printString, ' é um número válido em Smalltalk'.
    Transcript cr.
    contador := contador + 1.
].
```

Neste exemplo, usamos o laço de repetição `whileTrue:` para imprimir os números de 1 a 10 na janela Transcriber, que é uma ferramenta de depuração no ambiente de desenvolvimento Smalltalk. A cada iteração do laço, o número é incrementado em 1 e é verificado se ainda está dentro do intervalo desejado.

Note que o código está em inglês, mas você pode traduzir as mensagens de saída para o português, se necessário.