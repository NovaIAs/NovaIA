```d
import std.algorithm, std.container, std.stdio, std.string, std.traits;
import std.conv, std.exception, std.file, std.foreach, std.functional, std.range;
import std.stdio, std.unicode;
import std.algorithm, std.c.stdio, std.error, std.exception, std.file, std.numeric;
import std.parallelism, std.stdio, std.string, std.traits, std.typecons, std.utf.codepoint;
import std.algorithm, std.ascii, std.bigint, std.c.math, std.c.stdio, std.conv, std.file;
import std.fixedpoint, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.c.stdio, std.complex, std.exception, std.file, std.foreach;
import std.functional, std.math, std.numeric, std.parallelism, std.range, std.stdio;
import std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.file;
import std.foreach, std.functional, std.math, std.numeric, std.parallelism, std.range;
import std.stdio, std.string, std.traits, std.utf.codepoint;
import std.algorithm, std.array, std.c.stdio, std.container, std.exception, std.