```css
/* CSS Reset */

/* Box sizing rules */
*,
*::before,
*::after {
  box-sizing: border-box;
}

/* Remove margins and paddings from the <body> element */
body {
  margin: 0;
  padding: 0;
}

/* Set the default font family */
body {
  font-family: Arial, sans-serif;
}

/* Set the default font size */
body {
  font-size: 16px;
}

/* Set the default line height */
body {
  line-height: 1.5;
}

/* Remove the default margin on headings */
h1,
h2,
h3,
h4,
h5,
h6 {
  margin: 0;
}

/* Set the default font weight for headings */
h1,
h2,
h3,
h4,
h5,
h6 {
  font-weight: bold;
}

/* Set the default font size for headings */
h1 {
  font-size: 2rem;
}

h2 {
  font-size: 1.5rem;
}

h3 {
  font-size: 1.25rem;
}

h4 {
  font-size: 1rem;
}

h5 {
  font-size: 0.875rem;
}

h6 {
  font-size: 0.75rem;
}

/* Set the default margin and padding for paragraphs */
p {
  margin: 0;
  padding: 0;
}

/* Set the default font size for paragraphs */
p {
  font-size: 1rem;
}

/* Set the default line height for paragraphs */
p {
  line-height: 1.5;
}

/* Set the default margin and padding for lists */
ul,
ol {
  margin: 0;
  padding: 0;
}

/* Remove the default list style for unordered lists */
ul {
  list-style-type: none;
}

/* Set the default font size for list items */
li {
  font-size: 1rem;
}

/* Set the default line height for list items */
li {
  line-height: 1.5;
}

/* Set the default margin and padding for images */
img {
  margin: 0;
  padding: 0;
}

/* Set the default display style for images */
img {
  display: block;
}

/* Set the default width and height for images */
img {
  width: 100%;
  height: auto;
}

/* Set the default border style for images */
img {
  border: none;
}

/* Set the default alt attribute for images */
img {
  alt: "";
}

/* Set the default margin and padding for links */
a {
  margin: 0;
  padding: 0;
}

/* Set the default font size for links */
a {
  font-size: 1rem;
}

/* Set the default line height for links */
a {
  line-height: 1.5;
}

/* Set the default color for links */
a {
  color: #000;
}

/* Set the default text decoration for links */
a {
  text-decoration: none;
}

/* Set the default hover state for links */
a:hover {
  text-decoration: underline;
}

/* Set the default active state for links */
a:active {
  color: #f00;
}

/* Set the default visited state for links */
a:visited {
  color: #800;
}

/* Set the default focus state for links */
a:focus {
  outline: none;
}

/* Typography */

/* Headings */

h1 {
  font-size: 2.5rem;
  font-weight: bold;
  margin-bottom: 1rem;
}

h2 {
  font-size: 2rem;
