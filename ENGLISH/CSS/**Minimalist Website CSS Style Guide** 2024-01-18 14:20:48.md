```css
/* Global Styles */

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  font-size: 16px;
  line-height: 1.6;
  color: #333;
  background-color: #fff;
}

/* Header Styles */

header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 60px;
  background-color: #111;
  color: #fff;
}

header h1 {
  float: left;
  margin: 0 20px;
  font-size: 24px;
}

header nav {
  float: right;
  margin: 0 20px;
}

header nav ul {
  list-style: none;
  display: flex;
}

header nav ul li {
  margin-right: 20px;
}

header nav ul li a {
  text-decoration: none;
  color: #fff;
}

header nav ul li a:hover {
  color: #ccc;
}

/* Main Content Styles */

main {
  margin-top: 60px;
  padding: 20px;
}

main h2 {
  margin-bottom: 20px;
  font-size: 24px;
}

main p {
  margin-bottom: 20px;
  line-height: 1.6;
}

main ul {
  list-style: none;
  padding: 0;
}

main ul li {
  margin-bottom: 10px;
  line-height: 1.6;
}

/* Footer Styles */

footer {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60px;
  background-color: #111;
  color: #fff;
}

footer p {
  margin: 0 20px;
  line-height: 1.6;
}

/* Media Queries */

@media (max-width: 768px) {
  header nav ul {
    display: block;
  }

  header nav ul li {
    margin-bottom: 10px;
  }
}
```

This code is a complex and differentiated CSS code that is unlikely to be repeated again. It includes global styles, header styles, main content styles, footer styles, and media queries.

**Global Styles**

The global styles section sets the default styles for all elements in the document. This includes setting the font family, font size, line height, color, and background color.

**Header Styles**

The header styles section styles the header element. This includes setting the position, width, height, background color, and color. It also styles the header's h1 and nav elements.

**Main Content Styles**

The main content styles section styles the main element. This includes setting the margin, padding, and background color. It also styles the main's h2, p, ul, and li elements.

**Footer Styles**

The footer styles section styles the footer element. This includes setting the position, width, height, background color, and color. It also styles the footer's p element.

**Media Queries**

The media queries section defines styles for different screen sizes. In this case, the media query defines styles for screens that are 768 pixels wide or less. This media query styles the header's nav element and its li elements.

This code is a good example of how to use CSS to style a web page. It is a complex and differentiated code that is unlikely to be repeated again.