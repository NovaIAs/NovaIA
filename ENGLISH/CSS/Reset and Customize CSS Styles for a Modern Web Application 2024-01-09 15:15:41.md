```css
/*
 * Reset CSS
 * This CSS file resets all browser default styles.
 * It's a good starting point for building a consistent and modern web application.
 */

/*
 * Set the default font family.
 */
body {
  font-family: 'Helvetica Neue', 'Helvetica', 'Arial', sans-serif;
}

/*
 * Set the default font size.
 */
body {
  font-size: 16px;
}

/*
 * Set the default line height.
 */
body {
  line-height: 1.6;
}

/*
 * Set the default margin and padding.
 */
body {
  margin: 0;
  padding: 0;
}

/*
 * Reset the default appearance of headings.
 */
h1, h2, h3, h4, h5, h6 {
  font-weight: normal;
  margin-top: 0;
  margin-bottom: 16px;
}

/*
 * Reset the default appearance of paragraphs.
 */
p {
  margin-top: 0;
  margin-bottom: 16px;
}

/*
 * Reset the default appearance of links.
 */
a {
  color: #337ab7;
  text-decoration: none;
}

/*
 * Reset the default appearance of lists.
 */
ul, ol {
  list-style-type: none;
  padding: 0;
  margin: 0;
}

/*
 * Reset the default appearance of images.
 */
img {
  max-width: 100%;
  height: auto;
}

/*
 * Reset the default appearance of tables.
 */
table {
  border-collapse: collapse;
  border-spacing: 0;
}

/*
 * Reset the default appearance of forms.
 */
form {
  margin: 0;
  padding: 0;
}

/*
 * Reset the default appearance of input fields.
 */
input[type="text"], input[type="email"], input[type="password"] {
  width: 100%;
  height: 38px;
  padding: 6px 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

/*
 * Reset the default appearance of textarea fields.
 */
textarea {
  width: 100%;
  height: 150px;
  padding: 6px 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

/*
 * Reset the default appearance of buttons.
 */
button {
  display: inline-block;
  padding: 6px 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
  cursor: pointer;
}

/*
 * Custom CSS
 * This CSS file contains custom styles for your web application.
 */

/*
 * Set the default background color.
 */
body {
  background-color: #f5f5f5;
}

/*
 * Set the default color for headings.
 */
h1, h2, h3, h4, h5, h6 {
  color: #333;
}

/*
 * Set the default color for paragraphs.
 */
p {
  color: #666;
}

/*
 * Set the default color for links.
 */
a {
  color: #337ab7;
}

/*
 * Set the default color for lists.
 */
ul, ol {
  color: #666;
}

/*
 * Set the default color for images.
 */
img {
  border: 1px solid #ccc;
}

/*
 * Set the default color for tables.
 */
table {
  border-color: #ccc;
}

/*
 * Set the default color for forms.
 */
form {
  background-color: #fff;
}

/*
 * Set the default color for input fields.
 */
input[type="text"], input[type="email"], input[type="password"] {
  color: #333;
}

/*
 * Set the default color for textarea fields.
 */
textarea {
  color: #333;
}

/*
 * Set the default color for buttons.
 */
button {
  color: #fff;
  background-color: #337ab7;