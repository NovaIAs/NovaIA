(define cond and or not)
(define (+ 3 4) 7)
(define (- 3 4) -1)
(define (* 3 4) 12)
(define (/ 3 4) 0)
(define (max a b) (if (> a b) a b))
(define (min a b) (if (< a b) a b))
(define (abs x) (if (< x 0) (- x) x))
(define (sqrt x) (sqrt x))
(define (pow x n) (expt x n))
(define (log x) (log x))
(define (sin x) (sin x))
(define (cos x) (cos x))
(define (tan x) (tan x))
(define (asin x) (asin x))
(define (acos x) (acos x))
(define (atan x) (atan x))
(define (sinh x) (sinh x))
(define (cosh x) (cosh x))
(define (tanh x) (tanh x))
(define (asinh x) (asinh x))
(define (acosh x) (acosh x))
(define (atanh x) (atanh x))
(define (ceil x) (ceiling x))
(define (floor x) (floor x))
(define (round x) (round x))
(define (truncate x) (truncate x))
(define (is-integer? x) (integer? x))
(define (is-real? x) (real? x))
(define (is-complex? x) (complex? x))
(define (is-null? x) (null? x))
(define (is-list? x) (list? x))
(define (is-vector? x) (vector? x))
(define (is-string? x) (string? x))
(define (is-symbol? x) (symbol? x))
(define (is-port? x) (port? x))
(define (is-procedure? x) (procedure? x))
(define (is-promise? x) (promise? x))
(define (is-continuation? x) (continuation? x))
(define (is-input-port? x) (input-port? x))
(define (is-output-port? x) (output-port? x))
(define (is-bytevector? x) (bytevector? x))
(define (is-string-vector? x) (string-vector? x))
(define (is-vector-ref? x) (vector-ref? x))
(define (is-mutable? x) (mutable? x))
(define (is-immutable? x) (immutable? x))
(define (is-exact? x) (exact? x))
(define (is-inexact? x) (inexact? x))
(define (is-finite? x) (finite? x))
(define (is-infinite? x) (infinite? x))
(define (is-nan? x) (nan? x))
(define (is-positive? x) (positive? x))
(define (is-negative? x) (negative? x))
(define (is-zero? x) (zero? x))
(define (is-even? x) (even? x))
(define (is-odd? x) (odd? x))
(define (is-true? x) (true? x))
(define (is-false? x) (false? x))
(define (car x) (car x))
(define (cdr x) (cdr x))
(define (cons x y) (cons x y))
(define (list) (list))
(define (list? x) (list? x))
(define (length x) (length x))
(define (append x y) (append x y))
(define (reverse x) (reverse x))
(define (member x y) (member x y))
(define (assoc x y) (assoc x y))
(define (ass